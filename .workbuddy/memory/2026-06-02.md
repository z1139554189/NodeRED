# 2026-06-02

## Dashboard 断开修复（第二次）
- **问题**：dashboard.html 更新后看板显示"断开"
- **原因**：6 个 fetch 路径被回退成 `BASE + '/api/v1/...'`（直调 Bridge 8000），应从 Node-RED 走代理 `/api/proxy/...`
- **修复**：全部改回 `PROXY + '/health'` / `PROXY + '/batch-read'` / `PROXY + '/query'` / `PROXY + '/export'` / `PROXY + '/cache-stats'`
- **教训**：每次编辑 dashboard.html 必须检查全部 fetch 路径

## 技术讨论
- 用户确认心跳 10 秒写入机制使"离桶起始最近的值"逻辑在大部分场景下正确
- 09:59:59 变为 420.1，10:00:20 变为 423 → 桶内有心跳 10:00:10=420.1，正确取 420.1

## client.py 同步
- OPC-UA-Bridge `client.py` V7.1.0 → `.node-red/opcua_api_bridge/` 同步
- 核心改动：`_SubHandler.datachange_notification` 回调中立即更新 `_value_cache`（看板 0.5s 轮询即可读到最新值）
- `.node-red` 旧版缺此优化，已补齐
- `main.py` 两边各有对方的改进（测试路由 vs FIQ kg 等），属有意分化，未同步

## main.py 同步
- OPC-UA-Bridge `main.py` → `.node-red/opcua_api_bridge/` 同步
- 三处统一：
  1. `interval_seconds`：`ge=1` → `ge=0`（允许全量原始数据导出）
  2. Excel 表头备注单位：`FIQ (kg)` / `FIT (kg/h)` / `ERR (0/1)`
  3. Excel 数据格格式：去掉 `0.00" kg"`，统一 `0.00`
- 唯一剩余差异：OPC-UA-Bridge 的 3 个测试路由（.node-red 不需要）
