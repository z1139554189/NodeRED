# 2026-05-21

## 安装 node-red-manager Skill
- 从 ClawHub 查找并安装了 `node-red-manager` (by 1999azzar, v1.0.0, MIT-0)
- 安装路径: `~/.workbuddy/skills/node-red-manager/`
- 安全审计: 总体中等风险（2 Medium + 4 Low），均为功能特性导致（完整管理员权限），非恶意代码
- 功能: 通过 Node-RED Admin API 管理 flows 部署/备份/恢复、节点模块管理、运行时诊断、上下文读写
- 配置要求: 需设置 NODE_RED_URL、NODE_RED_USERNAME、NODE_RED_PASSWORD 环境变量（.env 文件）
- 注意: Node-RED settings.js 需开启 Admin API 认证才能使用

## 安装 Node-RED 并配置 Admin API
- 系统重装后 Node-RED 丢失，重新安装: `npm install -g node-red` → v4.1.10
- 首次运行生成 `.node-red` 目录: `C:\Users\Administrator\.node-red\`
- 生成 bcrypt 密码哈希 (Admin_00): `$2b$08$4fi/bybvu3SPBPblm09PM.Kk6XJoMLnaQsK6NyBSU06xJTX2BvHLq`
- 修改 `settings.js`: 开启 `adminAuth` (type: credentials, username: admin, password: Admin_00)
- 设置 `credentialSecret`: "opcua-cloud-nodered-2026"
- 创建 Skill `.env`: `NODE_RED_URL=http://localhost:1880`, `NODE_RED_USERNAME=admin`, `NODE_RED_PASSWORD=Admin_00`
- Node-RED 已启动运行在 `http://127.0.0.1:1880`
- 安装 bcryptjs 到全局 npm 用于密码哈希生成
